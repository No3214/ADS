# kads sıfır zorunlu çalışma bağımlılığıyla çalışır (stdlib).
# Geliştirme/test için:
pytest>=7
# Opsiyonel (config'i PyYAML ile okumak isterseniz; kads mini-parser ile de çalışır):
# pyyaml>=6

# Real API Integration
google-ads>=23.0.0
facebook-business>=19.0.0

# API & Backend
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
sqlalchemy>=2.0.0

# Scheduler
apscheduler>=3.10.0
